/*
** EPITECH PROJECT, 2017
** buttons.c
** File description:
** MY RPG 2017 | Hugo Castelli x Constant Loubier x Tais Laraaj x Pierre Herman
*/

#include "my.h"

void mouse_over_pause(map_t *map)
{
	mouse_over_pause1(map);
	mouse_over_pause2(map);
}

void mouse_click_pause(map_t *map)
{
	mouse_click_pause1(map);
}

void mouse_over(map_t *map)
{
	mouse_over1(map);
	mouse_over2(map);
	mouse_over3(map);
}

void mouse_click(map_t *map)
{
	mouse_click1(map);
	mouse_click2(map);
	mouse_click3(map);
}
